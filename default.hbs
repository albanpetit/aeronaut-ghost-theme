<!DOCTYPE html>
<html lang="{{@site.locale}}" class="h-full">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="{{asset "built/index.css"}}" />
    <script src="{{asset "built/index.js"}}"></script>

    <title>{{ meta_title }}</title>

    {{ ghost_head }}
  </head>
  <body class="{{ body_class }} relative  mx-auto p-4 sm:p-6 text-base-content container">
    <header class="">
      <nav class="navbar">
        <div class="navbar-start flex">
          <div class="flex gap-4">
            <img class="w-32" src="{{@site.logo}}" alt="{{@site.title}}">
          </div>
        </div>
        <div class="navbar-end">
          {{ navigation }}
        </div>
      </nav>
    </header>

    <main class="flex flex-col gap-4 sm:gap-6 my-4 sm:my-6">
      {{{ body }}}
    </main>

    {{ ghost_foot }}
  </body>
</html>
